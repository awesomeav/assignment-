export enum FieldType {
  Input = 'input',
  Select = 'select',
  Checkbox = 'checkbox',
  Radio = 'radio',
  UploadImage = 'uploadImage',
  DateTime = 'DateTime',
  InputSelect = 'inputSelect'
}
